import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.expenses.application',
  appName: 'Expenses EC',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
