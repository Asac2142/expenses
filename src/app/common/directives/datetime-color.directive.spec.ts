import { DatetimeColorDirective } from './datetime-color.directive';

describe('DatetimeColorDirective', () => {
  it('should create an instance', () => {
    const directive = new DatetimeColorDirective();
    expect(directive).toBeTruthy();
  });
});
